import LoginForm from "./LoginForm";
export const metadata = { title: "Acceso privado | TechPaws", robots: { index: false, follow: false } };
export default function AdminLogin() { return <main className="admin-login"><LoginForm /></main>; }
